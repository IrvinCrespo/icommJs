var mongoose = require('mongoose');
var connection = require('./connection');
var Schema = mongoose.Schema, ObjectId = Schema.ObjectId;
var Model = mongoose.Model

module.exports = class ModelGeneric extends Model{
    

    constructor(classObj,name,schema){
        mongoose.Promise = Promise;
        schema.loadClass(classObj);
        var ModelBuild = connection.model(name,schema,name);
        super(ModelBuild)
    }
    onSuccess(){
    }

    onError(){
    }
    
}